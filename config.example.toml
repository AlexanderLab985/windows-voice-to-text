[api]
# Primary provider: "groq" or "openai".
provider = "groq"

# 1. Create a Groq API key: https://console.groq.com/keys
# 2. Copy this file to config.toml
# 3. Paste your key between the quotes below.
groq_api_key = "paste-your-groq-api-key-here"

# Optional fallback. If Groq fails and this key is set, the app will try OpenAI Whisper.
# Create an OpenAI API key: https://platform.openai.com/api-keys
openai_api_key = ""

[hotkey]
# Push-to-talk keys. All listed keys must be held simultaneously.
# Single-key examples: ["ctrl_r"], ["f12"], ["pause"]
# Combo examples:      ["ctrl_r", "space"], ["alt_r", "space"]
# Available: ctrl_r, ctrl_l, alt_r, alt_l, alt_gr, shift_r, shift_l,
#            space, f1..f12, pause, scroll_lock
keys = ["ctrl_r"]

[audio]
sample_rate = 16000
channels = 1

[ui]
accent_color = "#5DADE2"
